
import 'package:flutter/material.dart';

class PhotoScannerPage extends StatelessWidget {
  const PhotoScannerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        width: 100,
        height: 100,
        color: Colors.blue,
        child: Text("photo page"),
      ),
    );
  }
}